from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='apply pipeline, data versioning, code versioning, experiment tracking using DVC',
    author='adnan',
    license='MIT',
)
